# require 'spec_helper'

# describe Interest do
  # let(:interest_table) { FactoryGirl.build :interest }
  # subject { :interest_table }

  # it { should validate_length_of(:interest).is_at_most(200) }
  # it { should validate_presense_of :interest }
  # it { should ensure_inclusion_of(:interest).in_array(Interest::INTERESTS) }
  # it { should belong_to :contact }
# end
